import React from 'react'

function ThirdColumn({id, amount}:{id:string, amount:number}) {
  
  console.log(id);
  console.log(amount);
  
  

  return (
    <div>ThirdColumn</div>
  )
}

export default ThirdColumn